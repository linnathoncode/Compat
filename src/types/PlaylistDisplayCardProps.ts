export type PlaylistDisplayCardProps = {
  playlistName: string;
  //   playlistData: any, //define a props specifically for this one later
  ownerName: string;
  trackCount: number;
  spotifyUrl: string;
  imageUrl: string;
};
